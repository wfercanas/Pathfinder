import React from 'react';
import { Finder } from '.';

export default {
  title: 'Components/Finder',
  component: Finder,
};

const EmptyFinder = (args) => <Finder {...args} />;

export { EmptyFinder };
